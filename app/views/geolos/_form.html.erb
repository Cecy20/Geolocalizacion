<%= form_with(model: geolo, local: true) do |form| %>
  <% if geolo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(geolo.errors.count, "error") %> prohibited this geolo from being saved:</h2>

      <ul>
      <% geolo.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :latitude %>
    <%= form.text_field :latitude, id: :geolo_latitude %>
  </div>

  <div class="field">
    <%= form.label :longitude %>
    <%= form.text_field :longitude, id: :geolo_longitude %>
  </div>

  <div class="field">
    <%= form.label :address %>
    <%= form.text_field :address, id: :geolo_address %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_area :description, id: :geolo_description %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title, id: :geolo_title %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
